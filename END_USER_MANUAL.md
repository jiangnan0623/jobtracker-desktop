# 个人求职助手用户使用说明

## 一、软件介绍

个人求职助手是一款 Windows 桌面客户端，用来集中管理求职过程中的岗位投递、招聘进度、简历文件、面试记录、笔记和提醒事项。

你可以用它记录：

- 投递了哪些公司和岗位
- 每个岗位当前进行到哪一步
- 每个岗位使用了哪份简历
- 岗位 JD、投递链接、来源和备注
- 一面、二面、主管面、HR 面等流程结果
- 面试记录和面试复盘笔记
- 普通学习笔记、资料笔记、PDF/Word/TXT/Markdown 文件
- 后续需要完成的提醒事项

软件数据默认保存在本机，不需要远程服务器。

## 二、选择哪个安装包

当前提供两个安装包。

| 安装包 | 推荐对象 | 是否需要安装 Java | 说明 |
| --- | --- | --- | --- |
| `个人求职助手 Setup 0.1.0-full.exe` | 大多数用户 | 不需要 | 已内置 Java 运行环境，推荐使用 |
| `个人求职助手 Setup 0.1.0-lite.exe` | 已安装 Java 17 的用户 | 需要 | 安装包更小，适合电脑已有 Java 17 的情况 |

如果你不确定自己的电脑有没有 Java，直接使用 `full` 版。

两个安装包是同一个软件。已经安装了其中一个版本后，再安装另一个版本，相当于覆盖升级，通常不会影响已有数据。

## 三、安装和启动

1. 双击安装包，例如：

```text
个人求职助手 Setup 0.1.0-full.exe
```

2. 按安装向导完成安装。

3. 安装完成后，通过桌面快捷方式或开始菜单打开：

```text
个人求职助手
```

4. 软件启动后会自动打开主界面。

如果使用 `lite` 版且打不开，请确认电脑已经安装 Java 17。

## 四、首次使用建议

第一次打开后，建议先进入左侧菜单：

```text
系统设置
```

设置三个保存目录：

- 简历保存目录
- 面试笔记保存目录
- 普通笔记保存目录

建议放在容易备份的位置，例如：

```text
D:\求职材料\resumes
D:\求职材料\interview_notes
D:\求职材料\notes
```

如果不设置，软件会使用默认目录保存数据和文件。

## 五、主界面说明

左侧是功能菜单：

- `Dashboard`：查看整体求职数据
- `投递管理`：管理岗位投递记录
- `简历管理`：上传和维护简历文件
- `笔记管理`：管理普通笔记和资料文件
- `提醒管理`：管理待办提醒
- `系统设置`：设置文件保存位置

右侧是当前页面内容。

## 六、Dashboard 使用说明

Dashboard 是总览页，可以快速查看：

- 总投递数
- 面试数量
- Offer 数量
- 投递状态分布
- 每周投递趋势
- 公司投递统计
- 最近投递记录
- 今日提醒

如果存在未完成提醒，软件会显示未完成预警，直到提醒被标记完成。

## 七、投递管理

进入：

```text
投递管理
```

可以新增、编辑、删除岗位投递记录。

### 1. 新增投递

点击 `新增`，填写信息：

- 公司名称
- 岗位名称
- 岗位类别
- 投递批次
- 简历类别
- 当前状态
- 绑定简历
- 投递时间
- 来源、链接、地点、薪资
- 岗位 JD
- 备注

岗位类别、简历类别、投递批次和状态都支持选择，也支持自定义。

### 2. 查询和排序

支持按以下条件查询：

- 公司名
- 投递状态
- 岗位类别
- 投递批次
- 简历类别
- 投递时间范围

支持排序：

- 投递时间倒序
- 投递时间正序
- 公司名 A-Z
- 公司名 Z-A

也可以开启 `按公司合并`，把同一公司的多个岗位折叠到一起查看。

## 八、投递详情

在投递列表中点击某条岗位记录，可以进入详情页。

详情页可以查看和维护：

- 岗位基础信息
- 绑定简历
- 岗位 JD 预览
- 投递进度时间轴
- 当前进度、成功/失败结果、操作时间
- 自定义招聘流程
- 面试记录
- Markdown 面试笔记

### 1. 管理投递进度

点击时间轴中的某个流程节点，会出现进度管理区域。

可以设置：

- 当前进度
- 结果：进行中、成功、失败
- 操作时间

保存后，顶部状态和时间轴会同步更新。

### 2. 自定义招聘流程

不同公司流程可能不同，可以点击 `编辑流程` 自定义阶段，例如：

```text
简历筛选
笔试
一面
二面
主管面
HR 面
Offer
淘汰
```

自定义后，岗位状态会从该岗位自己的流程中选择，避免状态和进度条不一致。

### 3. 面试记录

在 `面试流程` 区域可以新增面试记录，记录：

- 面试轮次
- 面试时间
- 面试官
- 面试问题
- 面试结果
- 难度
- 总结

### 4. 面试笔记

在 `Markdown 面试笔记` 区域可以新增面试复盘笔记。

支持：

- 新增
- 预览
- 编辑
- 下载
- 删除

面试笔记和普通笔记是分开保存的。

## 九、简历管理

进入：

```text
简历管理
```

可以管理不同版本的简历。

支持：

- 上传简历
- 编辑版本名称和备注
- PDF 简历预览
- 下载简历
- 删除简历

建议给简历起清晰名称，例如：

```text
Java 后端简历
AI 工程简历
国企通用简历
银行金融科技简历
```

在新增或编辑投递记录时，可以绑定对应简历。

## 十、笔记管理

进入：

```text
笔记管理
```

这里像一个简单的文件管理器，用于管理普通笔记和资料文件。

### 1. 支持的文件类型

| 文件类型 | 能否预览 | 能否编辑内容 |
| --- | --- | --- |
| Markdown：`.md` / `.markdown` | 可以 | 可以 |
| 文本：`.txt` | 可以 | 可以 |
| PDF：`.pdf` | 可以 | 不可以 |
| Word：`.doc` / `.docx` | 可以预览提取文本 | 不可以 |

PDF 和 Word 不能直接编辑正文内容，但可以编辑文件信息，例如名称和所在位置。

### 2. 新建文件夹

点击 `新建文件夹`，可以选择：

- 文件夹名称
- 所在位置
- 本地保存位置

如果给文件夹设置了本地保存位置，放入该文件夹的新笔记会保存到对应目录。

### 3. 新建或上传笔记

可以点击：

- `新建笔记`
- `上传笔记`

上传笔记支持 Markdown、TXT、PDF、Word 文件。

### 4. 预览和编辑

点击左侧文件后，右侧默认直接显示预览内容。

如果是 Markdown 或 TXT，可以点击 `编辑内容` 修改正文。

如果需要修改名称、位置等信息，点击 `文件信息`。

### 5. 移动文件和文件夹

支持：

- 拖拽笔记到文件夹
- 拖拽文件夹到其他文件夹
- 点击 `移到根目录`
- 拖拽到左侧 `移出到根目录` 区域

移动后，左侧文件树会同步更新嵌套关系。

## 十一、提醒管理

进入：

```text
提醒管理
```

可以新增待办提醒，例如：

- 明天 10 点完成笔试
- 周五前跟进 HR
- 下周一整理面试复盘

提醒支持：

- 新增
- 编辑
- 标记完成
- 删除

未完成提醒会高亮显示，并触发全局未完成预警。

## 十二、数据保存和备份

默认数据通常保存在：

```text
C:\Users\你的用户名\AppData\Roaming\个人求职助手\
```

重要内容包括：

```text
jobtracker.mv.db
resumes\
interview_notes\
notes\
storage.properties
```

如果你在系统设置里改过保存目录，也要备份那些自定义目录。

建议定期备份整个数据目录，尤其是在重装系统或更换电脑前。

## 十三、换电脑怎么办

迁移数据可以按以下步骤：

1. 在旧电脑关闭客户端。
2. 备份旧电脑的数据目录。
3. 在新电脑安装客户端。
4. 关闭新电脑客户端。
5. 将旧电脑数据复制到新电脑对应目录。
6. 如果设置过自定义保存目录，也一起复制。
7. 重新打开客户端检查数据。

## 十四、常见问题

### 1. 客户端打不开怎么办？

如果使用 `full` 版，通常不需要额外环境。可以尝试：

1. 重新安装 `full` 版安装包。
2. 关闭安全软件后再安装一次。
3. 在任务管理器中结束残留的 `个人求职助手` 或 `java.exe` 进程。
4. 重新启动电脑。

如果使用 `lite` 版，请确认电脑有 Java 17。

### 2. lite 版和 full 版可以互相覆盖安装吗？

可以。

两个版本是同一个应用身份，互相安装相当于覆盖升级，通常会继续使用原来的本地数据。

注意：如果从 `full` 版换成 `lite` 版，电脑必须有 Java 17，否则客户端可能无法启动。

### 3. 修改保存位置后，旧文件会自动移动吗？

不会。

保存位置只影响之后新上传或新创建的文件。旧文件不会自动迁移，避免误移动你的资料。

### 4. Word 为什么不能像 Word 软件一样编辑？

客户端只做 Word 内容预览和文件管理，不提供 Word 原格式编辑。

如需编辑 Word 内容，请下载后用 Microsoft Word 或 WPS 修改，再重新上传。

### 5. PDF 为什么不能编辑？

PDF 是固定版式文件，客户端提供预览和下载，不直接编辑 PDF 正文。

如需修改 PDF，请使用专业 PDF 编辑工具后重新上传。

### 6. 数据会上传到服务器吗？

不会。

当前桌面版默认本地运行，数据保存在本机数据库和本地文件目录中。

## 十五、推荐使用习惯

建议这样使用：

1. 每投递一个岗位，及时新增记录。
2. 每条投递绑定实际使用的简历。
3. 面试后马上补充面试记录和复盘笔记。
4. 用提醒管理跟进笔试、面试、HR 沟通等事项。
5. 每周备份一次数据目录。

这样后续复盘求职进度、准备面试、整理材料都会更清晰。
